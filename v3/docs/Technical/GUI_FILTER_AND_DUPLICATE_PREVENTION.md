# v3.3.0 - GUI フィルタ機能と重複投稿防止オプション

**対象バージョン**: v3.1.0+（v3.3.0 対応版）
**最終更新**: 2026-01-03
**ステータス**: ✅ 実装完了・本番対応

---

## 📖 目次

1. [概要](#概要)
2. [GUI フィルタ機能](#gui-フィルタ機能)
3. [重複投稿防止オプション](#重複投稿防止オプション)
4. [使用方法](#使用方法)
5. [トラブルシューティング](#トラブルシューティング)
6. [技術詳細](#技術詳細)

---

## 概要

v3.1.0 で導入され、v3.3.0 で拡張された 2 つの主要機能：

### 1. **GUI フィルタ機能**
GUI の動画一覧から、複数条件を組み合わせて動画をフィルタリングできます。

- 📝 **タイトル検索**: キーワードで動画名を検索
- 📊 **投稿状態フィルタ**: 投稿済み/未投稿で絞り込み
- 🌐 **配信元フィルタ**: YouTube/ニコニコなど配信元で絞り込み
- 🎬 **コンテンツタイプフィルタ** (v3.3.0 NEW): 動画種別で絞り込み（動画/アーカイブ/放送予約/放送中/放送終了/プレミア）

### 2. **重複投稿防止オプション**
同じ動画を誤って 2 度以上 Bluesky に投稿することを防ぎます。

- ⚙️ **設定による制御**: settings.env で有効/無効を切り替え
- ⚠️ **事前警告**: 重複投稿を検知したら警告メッセージを表示
- 🛡️ **安全な投稿**: 重複投稿の場合は投稿を中止

---

## GUIフィルタ・検索機能

### 概要

ツールバーの下に**フィルタパネル**が追加されました。以下の 4 つの条件で動画を絞り込めます：

```
🔍 フィルタ設定
┌──────────────────────────────────────────────────────────────────┐
│ タイトル検索: [          ]                                        │
│ 投稿状態: [全て▼]  配信元: [全て▼]  タイプ: [全て▼]  🔄 リセット │
└──────────────────────────────────────────────────────────────────┘
```

### フィルタ条件

#### 1. タイトル検索
- **入力欄**: テキストボックス
- **入力方法**: キーワードを入力
- **動作**: リアルタイム検索（入力中に即反応）
- **判定**: 大文字小文字を区別しない、部分一致

**例**：
```
入力: "新作"  → タイトルに「新作」を含む動画のみ表示
入力: "live"  → タイトルに「live」（大文字小文字区別なし）を含む動画を表示
```

#### 2. 投稿状態フィルタ
- **選択肢**: 「全て」「投稿済み」「未投稿」
- **判定基準**: `posted_to_bluesky` フラグ

| 選択肢 | 表示対象 | 条件 |
|:--|:--|:--|
| 全て | すべての動画 | フィルタなし |
| 投稿済み | 投稿済みの動画 | `posted_to_bluesky = 1` |
| 未投稿 | 未投稿の動画 | `posted_to_bluesky = 0` |

**用途**:
- 「未投稿」で投稿予定の動画を確認
- 「投稿済み」で投稿済み動画を確認
- 誤った再投稿検出

#### 3. 配信元フィルタ
- **選択肢**: 「全て」「YouTube」「Niconico」
- **判定基準**: `source` フィールド

| 選択肢 | 表示対象 | 条件 |
|:--|:--|:--|
| 全て | すべての動画 | フィルタなし |
| YouTube | YouTube 動画のみ | `source = "youtube"` |
| Niconico | ニコニコ動画のみ | `source = "niconico"` |

**用途**:
- YouTube のみ投稿したい場合
- ニコニコ動画のみ投稿したい場合
- 配信元ごとの管理

#### 4. コンテンツタイプフィルタ（v3.3.0 NEW）
- **選択肢**: 「全て」「🎬 動画」「📹 アーカイブ」「📅 放送予約」「🔴 放送中」「⏹️ 放送終了」「🎆 プレミア」
- **判定基準**: `content_type` + `live_status` + `is_premiere` フィールド

| 選択肢 | 対象 | 条件 |
|:--|:--|:--|
| 全て | すべての動画 | フィルタなし |
| 🎬 動画 | 通常動画 | `content_type = "video"` |
| 📹 アーカイブ | YouTube Live のアーカイブ | `content_type = "archive"` |
| 📅 放送予約 | YouTube の放送予約枠 | `content_type = "schedule"` |
| 🔴 放送中 | YouTube Live 配信中 | `content_type = "live"` |
| ⏹️ 放送終了 | YouTube Live 配信終了 | `content_type = "completed"` |
| 🎆 プレミア | プレミア配信動画 | `is_premiere = 1` |

**用途**:
- Live だけを投稿したい
- アーカイブだけを投稿したい
- プレミア動画を区別したい

### 複合フィルタ

4 つの条件をすべて満たす動画が表示されます：

```
【例】
タイトル検索: "新作"
投稿状態: "未投稿"
配信元: "YouTube"
タイプ: "🔴 放送中"

→ タイトルに「新作」を含む、未投稿の YouTube Live 配信中動画のみ表示
```

### リセットボタン

🔄 **リセット** ボタンをクリックすると、すべてのフィルタ条件をクリアして、元の状態に戻ります。

### ステータス表示

フィルタ適用時にステータスバーで件数を表示：

```
読み込み完了: 100 件中 25 件を表示（選択: 5 件）
```

- **100 件**: DB 内のすべての動画
- **25 件**: フィルタ条件に合致した動画
- **5 件**: 表示中の動画のうち、選択されている数

---

## 重複投稿防止オプション

### 概要

同じ動画を誤って 2 回以上投稿することを防止する機能です。

**状況例**：
- GUI 上で「投稿」ボタンを 2 回クリックしてしまった
- ユーザーが同じ動画を再度選択して投稿しようとした

**動作**：重複投稿を検知すると、警告メッセージを表示して投稿を中止します。

### 設定方法

#### 1. 設定ファイルを編集

`settings.env` を開き、以下の行を探します：

```env
# 重複投稿防止オプション（true/false、デフォルト: false）
# true に設定すると、同じ動画の再投稿を防止します。
PREVENT_DUPLICATE_POSTS=false
```

#### 2. 設定値を変更

```env
PREVENT_DUPLICATE_POSTS=true
```

#### 3. アプリケーションを再起動

変更を反映させるため、アプリケーションを再起動してください。

#### 4. ログで確認

起動ログに以下が表示されたら成功です：

```
重複投稿防止: 有効
```

### 有効化の条件

重複投稿防止機能が**実際に動作する条件**：

✅ **有効化される**:
- `PREVENT_DUPLICATE_POSTS=true` に設定
- **本投稿時のみ**（ドライラン時は実行しない）

❌ **無効化される**:
- `PREVENT_DUPLICATE_POSTS=false` の場合
- ドライラン（投稿テスト）実行時
- プラグイン非導入時

### 判定ロジック

重複投稿は以下で判定されます：

```
投稿ボタン クリック
  ↓
投稿前の確認画面で重複チェック
  ↓
DB で同じ video_id に posted_to_bluesky = 1 のレコードを検索
  ├─ 見つかった (重複) → ⚠️ 警告表示 → 投稿中止
  └─ 見つからない (初回) → ✅ 通常投稿
```

### ユーザーへの通知

重複投稿を検知した場合、以下のようなメッセージが表示されます：

```
⚠️ 警告: 重複投稿防止

この動画は既に投稿済みです。

【 新作動画を作成しました！ 】

重複投稿を防止しました。
```

### ログ出力

重複投稿を検知した場合、ログに以下が記録されます：

```
🛑 重複投稿を防止しました: dQw4w9WgXcQ
⚠️ 重複投稿検知: この動画は既に投稿済みです（dQw4w9WgXcQ）
```

---

## 使用方法

### GUIフィルタ・検索を使用した効率的な投稿方法

**シナリオ 1: 特定のテーマの動画のみ投稿したい**

```
1. タイトル検索欄に「ゲーム」と入力
2. 投稿状態フィルタで「未投稿」を選択
3. 表示された動画から投稿対象を選択
4. 「投稿設定」で投稿実行
```

**シナリオ 2: YouTube と Niconico を分けて投稿したい**

```
1. 配信元フィルタで「YouTube」を選択
2. YouTube 動画を投稿
3. フィルタをリセット
4. 配信元フィルタで「Niconico」を選択
5. ニコニコ動画を投稿
```

**シナリオ 3: 投稿済み動画を確認したい**

```
1. 投稿状態フィルタで「投稿済み」を選択
2. 投稿済み動画の一覧を表示
3. 投稿日時を確認
```

**シナリオ 4: YouTube Live 配信中の動画のみ投稿したい** (v3.3.0)

```
1. タイプフィルタで「🔴 放送中」を選択
2. YouTube Live 配信中の動画のみ表示
3. 選択して投稿
```

**シナリオ 5: プレミア配信とアーカイブを分けたい** (v3.3.0)

```
1. タイプフィルタで「🎆 プレミア」を選択して投稿
2. フィルタをリセット
3. タイプフィルタで「📹 アーカイブ」を選択して投稿
```

### 重複投稿防止を有効化して安全に投稿

```
1. settings.env を編集
   PREVENT_DUPLICATE_POSTS=true
2. アプリケーションを再起動
3. 以降、同じ動画の投稿は自動的に検知・防止
```

---

## トラブルシューティング

### Q1: フィルタが反映されない

**A**: 以下を確認してください：

1. **テキスト入力時**
   - テキストボックスにフォーカスがあるか確認
   - 入力後、キーが離される（KeyRelease イベント）まで待つ

2. **ドロップダウン選択時**
   - ドロップダウンで選択を変更したか確認
   - 「全て」で全フィルタをリセット

3. **複合フィルタ**
   - 複数条件を設定した場合、すべてを満たす動画のみ表示
   - 条件を満たす動画がない場合は表示が空

### Q2: フィルタをリセットしたい

**A**: 🔄 **リセット** ボタンをクリックしてください：

```
表示効果:
- タイトル検索欄がクリア
- 投稿状態フィルタが「全て」に
- 配信元フィルタが「全て」に
- すべての動画が表示

ログ出力:
✅ フィルタをリセットしました
```

### Q3: 重複投稿防止が機能しない

**A**: 以下の順序で確認してください：

1. **設定値を確認**
   ```env
   PREVENT_DUPLICATE_POSTS=true
   ```

2. **アプリケーションを再起動**
   - 設定は起動時に読み込まれるため

3. **ログで確認**
   ```
   起動ログ:
   重複投稿防止: 有効
   ```

4. **ドライラン時は実行しない**
   - 本投稿（「投稿設定」ボタン）で確認
   - ドライラン（「投稿テスト」ボタン）では実行しない

### Q4: 重複投稿防止を一時的に無効化したい

**A**: `settings.env` で以下に変更してから再起動：

```env
PREVENT_DUPLICATE_POSTS=false
```

ただし、再投稿時の誤入力にご注意ください。

### Q5: フィルタ適用後、動画が 1 件も表示されない

**A**: 以下の原因が考えられます：

1. **条件が厳しすぎる**
   - フィルタ条件を緩くしてみてください
   - 例: 投稿状態を「全て」に変更

2. **入力値が間違っている**
   - タイトル検索の大文字小文字は区別しません
   - 部分一致なので、完全な単語でなくても OK

3. **DB に該当する動画がない**
   - 「全て」で全動画を表示してから、条件を追加

## 技術詳細

### GUI フィルタ実装

#### ファイル: [gui_v3.py](../../gui_v3.py) (2740 行)

**フィルタパネル UI の追加** (行 190-220):

フィルタパネル構成：
- タイトル検索 Entry ウィジェット (`filter_title_var`)
- 投稿状態 Combobox (`filter_status_var`) - 全て/投稿済み/未投稿
- 配信元 Combobox (`filter_source_var`) - 全て/YouTube/Niconico
- コンテンツタイプ Combobox (`filter_type_var`) - 7 種類のタイプ (v3.3.0 新規)
- リセット Button (行 202)

**リアルタイムフィルタリング実装** (行 381-510):

`apply_filters()` メソッド：

```python
def apply_filters(self):
    """リアルタイムフィルタを適用"""
    title_filter = self.filter_title_var.get().lower()
    status_filter = self.filter_status_var.get()
    source_filter = self.filter_source_var.get().lower()
    type_filter = self.filter_type_var.get()

    filter_videos = []

    for video in self.all_videos:
        # タイトル検索フィルタ（部分一致、大文字小文字区別なし）
        if title_filter and title_filter not in video.get("title", "").lower():
            continue

        # 投稿状態フィルタ
        if status_filter != "全て":
            is_posted = video.get("posted_to_bluesky", 0) == 1
            if status_filter == "投稿済み" and not is_posted:
                continue
            if status_filter == "未投稿" and is_posted:
                continue

        # 配信元フィルタ
        if source_filter != "全て":
            video_source = video.get("source", "youtube").lower()
            if source_filter == "youtube" and video_source != "youtube":
                continue
            if source_filter == "niconico" and video_source != "niconico":
                continue

        # コンテンツタイプフィルタ (v3.3.0) - 複雑な表示型の判定
        if type_filter != "全て":
            content_type = video.get("content_type", "video")
            is_premiere = video.get("is_premiere", 0)
            source_for_display = video.get("source", "").lower()

            # display_type を計算（表示用）
            if is_premiere:
                display_type = "🎆 プレミア"
            elif source_for_display == "niconico":
                display_type = "🎬 動画"
            elif content_type == "archive":
                display_type = "📹 アーカイブ"
            elif content_type == "schedule":
                display_type = "📅 放送予約"
            elif content_type == "live":
                display_type = "🔴 放送中"
            elif content_type == "completed":
                display_type = "⏹️ 放送終了"
            else:
                display_type = "🎬 動画"

            if display_type != type_filter:
                continue

        # すべてのフィルタに合致した動画のみ追加
        filter_videos.append(video)

    # Treeview を更新（フィルタ後の動画を表示）
    self.treeview.delete(*self.treeview.get_children())
    for video in filter_videos:
        self.treeview.insert("", "end", values=(...))

    # ステータスバーを更新
    self.status_label.config(text=f"読み込み完了: {len(self.all_videos)} 件中 {len(filter_videos)} 件を表示")
```

**リセット機能** (行 504-510):

```python
def reset_filters(self):
    """すべてのフィルタをリセット"""
    self.filter_title_var.set("")
    self.filter_status_var.set("全て")
    self.filter_source_var.set("全て")
    self.filter_type_var.set("全て")
    self.apply_filters()
    logger.info("✅ フィルタをリセットしました")
```

### 重複投稿防止実装

#### ファイル: [config.py](../../config.py)

PREVENT_DUPLICATE_POSTS 設定の読み込み：

```python
# PREVENT_DUPLICATE_POSTS 環境変数を読み込み
prevent_duplicate_posts = os.getenv("PREVENT_DUPLICATE_POSTS", "false").lower() in ["true", "1", "yes", "on"]

# ログ出力
if prevent_duplicate_posts:
    logger.info("🛡️ 重複投稿防止: 有効")
else:
    logger.info("重複投稿防止: 無効")
```

#### ファイル: [database.py](../../database.py)

重複投稿検知メソッド：

```python
def is_duplicate_post(self, video_id: str) -> bool:
    """
    重複投稿かどうかをチェック

    Args:
        video_id: 動画ID

    Returns:
        bool: 重複投稿の場合 True、初回投稿の場合 False
    """
    try:
        conn = self._get_connection()
        cursor = conn.cursor()

        cursor.execute("""
            SELECT COUNT(*) FROM videos
            WHERE video_id = ? AND posted_to_bluesky = 1
        """, (video_id,))

        count = cursor.fetchone()[0]
        conn.close()

        is_duplicate = count > 0
        if is_duplicate:
            logger.warning(f"⚠️ 重複投稿検知: この動画は既に投稿済みです（{video_id}）")

        return is_duplicate

    except Exception as e:
        logger.error(f"重複チェック中にエラーが発生: {e}")
        return False
```

#### ファイル: [gui_v3.py](../../gui_v3.py) (2740 行)

投稿前の重複チェック (行 2665-2680):

```python
def _execute_post(self, dry_run=False):
    """投稿実行メソッド"""
    # ...
    video = self.get_selected_video()
    if not video:
        return False

    # ★ 重複投稿防止チェック (行 2665-2675)
    if self.config.prevent_duplicate_posts and not dry_run:
        if self.db.is_duplicate_post(video["video_id"]):
            # 警告メッセージボックスを表示
            messagebox.showwarning(
                "⚠️ 警告: 重複投稿防止",
                f"この動画は既に投稿済みです。\n\n【 {video['title'][:60]} 】\n\n重複投稿を防止するため、投稿を中止しました。"
            )
            logger.warning(f"🛑 重複投稿を防止しました: {video['video_id']}")
            return False  # 投稿中止

    # 通常投稿を実行 (行 2676-2680)
    if self.plugin_manager:
        success = self.plugin_manager.post_video_with_all_enabled(video, dry_run=dry_run)
    elif self.bluesky_core:
        success = self.bluesky_core.post_video_minimal(video)
    else:
        logger.error("投稿エラー: プラグインが見つかりません")
        return False

    # 投稿成功時に DB を更新
    if success and not dry_run:
        self.db.mark_as_posted(video["video_id"])

    return success
```

---

**作成日**: 2025-12-19
**最後の修正**: 2026-01-03
**ステータス**: ✅ 完成・本番対応

---

## 関連ドキュメント

- [GUIの使い方](../Guides/GUI_USER_MANUAL.md)
- [設定ファイル例](../../settings.env.example)
- [デバッグ・ドライランガイド](DEBUG_DRY_RUN_GUIDE.md)
