# v3.1.0 - GUI フィルタ機能と重複投稿防止オプション

**対象バージョン**: v3.1.0+
**最終更新**: 2025-12-19
**ステータス**: ✅ 実装完了・本番対応

---

## 📖 目次

1. [概要](#概要)
2. [GUI フィルタ機能](#gui-フィルタ機能)
3. [重複投稿防止オプション](#重複投稿防止オプション)
4. [使用方法](#使用方法)
5. [トラブルシューティング](#トラブルシューティング)
6. [技術詳細](#技術詳細)

---

## 概要

v3.1.0 では以下の 2 つの主要機能が追加されました：

### 1. **GUI フィルタ機能**
GUI の動画一覧から、複数条件を組み合わせて動画をフィルタリングできます。

- 📝 **タイトル検索**: キーワードで動画名を検索
- 📊 **投稿状態フィルタ**: 投稿済み/未投稿で絞り込み
- 🌐 **配信元フィルタ**: YouTube/ニコニコなど配信元で絞り込み

### 2. **重複投稿防止オプション**
同じ動画を誤って 2 度以上 Bluesky に投稿することを防ぎます。

- ⚙️ **設定による制御**: settings.env で有効/無効を切り替え
- ⚠️ **事前警告**: 重複投稿を検知したら警告メッセージを表示
- 🛡️ **安全な投稿**: 重複投稿の場合は投稿を中止

---

## GUIフィルタ・検索機能

### 概要

ツールバーの下に**フィルタパネル**が追加されました。以下の 3 つの条件で動画を絞り込めます：

```
🔍 フィルタ設定
┌──────────────────────────────────────────────────────────┐
│ タイトル検索: [         ]                                │
│ 投稿状態: [全て▼]  配信元: [全て▼]  🔄 リセット        │
└──────────────────────────────────────────────────────────┘
```

### フィルタ条件

#### 1. タイトル検索
- **入力欄**: テキストボックス
- **入力方法**: キーワードを入力
- **動作**: リアルタイム検索（入力中に即反応）
- **判定**: 大文字小文字を区別しない、部分一致

**例**：
```
入力: "新作"  → タイトルに「新作」を含む動画のみ表示
入力: "live"  → タイトルに「live」（大文字小文字区別なし）を含む動画を表示
```

#### 2. 投稿状態フィルタ
- **選択肢**: 「全て」「投稿済み」「未投稿」
- **判定基準**: `posted_to_bluesky` フラグ

| 選択肢 | 表示対象 | 条件 |
|:--|:--|:--|
| 全て | すべての動画 | フィルタなし |
| 投稿済み | 投稿済みの動画 | `posted_to_bluesky = 1` |
| 未投稿 | 未投稿の動画 | `posted_to_bluesky = 0` |

**用途**:
- 「未投稿」で投稿予定の動画を確認
- 「投稿済み」で投稿済み動画を確認
- 誤った再投稿検出

#### 3. 配信元フィルタ
- **選択肢**: 「全て」「YouTube」「Niconico」
- **判定基準**: `source` フィールド

| 選択肢 | 表示対象 | 条件 |
|:--|:--|:--|
| 全て | すべての動画 | フィルタなし |
| YouTube | YouTube 動画のみ | `source = "YouTube"` |
| Niconico | ニコニコ動画のみ | `source = "Niconico"` |

**用途**:
- YouTube のみ投稿したい場合
- ニコニコ動画のみ投稿したい場合
- 配信元ごとの管理

### 複合フィルタ

3 つの条件をすべて満たす動画が表示されます：

```
【例】
タイトル検索: "新作"
投稿状態: "未投稿"
配信元: "YouTube"

→ タイトルに「新作」を含む、未投稿の YouTube 動画のみ表示
```

### リセットボタン

🔄 **リセット** ボタンをクリックすると、すべてのフィルタ条件をクリアして、元の状態に戻ります。

### ステータス表示

フィルタ適用時にステータスバーで件数を表示：

```
読み込み完了: 100 件中 25 件を表示（選択: 5 件）
```

- **100 件**: DB 内のすべての動画
- **25 件**: フィルタ条件に合致した動画
- **5 件**: 表示中の動画のうち、選択されている数

---

## 重複投稿防止オプション

### 概要

同じ動画を誤って 2 回以上投稿することを防止する機能です。

**状況例**：
- GUI 上で「投稿」ボタンを 2 回クリックしてしまった
- ユーザーが同じ動画を再度選択して投稿しようとした

**動作**：重複投稿を検知すると、警告メッセージを表示して投稿を中止します。

### 設定方法

#### 1. 設定ファイルを編集

`settings.env` を開き、以下の行を探します：

```env
# 重複投稿防止オプション（true/false、デフォルト: false）
# true に設定すると、同じ動画の再投稿を防止します。
PREVENT_DUPLICATE_POSTS=false
```

#### 2. 設定値を変更

```env
PREVENT_DUPLICATE_POSTS=true
```

#### 3. アプリケーションを再起動

変更を反映させるため、アプリケーションを再起動してください。

#### 4. ログで確認

起動ログに以下が表示されたら成功です：

```
重複投稿防止: 有効
```

### 有効化の条件

重複投稿防止機能が**実際に動作する条件**：

✅ **有効化される**:
- `PREVENT_DUPLICATE_POSTS=true` に設定
- **本投稿時のみ**（ドライラン時は実行しない）

❌ **無効化される**:
- `PREVENT_DUPLICATE_POSTS=false` の場合
- ドライラン（投稿テスト）実行時
- プラグイン非導入時

### 判定ロジック

重複投稿は以下で判定されます：

```
投稿ボタン クリック
  ↓
投稿前の確認画面で重複チェック
  ↓
DB で同じ video_id に posted_to_bluesky = 1 のレコードを検索
  ├─ 見つかった (重複) → ⚠️ 警告表示 → 投稿中止
  └─ 見つからない (初回) → ✅ 通常投稿
```

### ユーザーへの通知

重複投稿を検知した場合、以下のようなメッセージが表示されます：

```
⚠️ 警告: 重複投稿防止

この動画は既に投稿済みです。

【 新作動画を作成しました！ 】

重複投稿を防止しました。
```

### ログ出力

重複投稿を検知した場合、ログに以下が記録されます：

```
🛑 重複投稿を防止しました: dQw4w9WgXcQ
⚠️ 重複投稿検知: この動画は既に投稿済みです（dQw4w9WgXcQ）
```

---

## 使用方法

### GUIフィルタ・検索を使用した効率的な投稿方法

**シナリオ 1: 特定のテーマの動画のみ投稿したい**

```
1. タイトル検索欄に「ゲーム」と入力
2. 投稿状態フィルタで「未投稿」を選択
3. 表示された動画から投稿対象を選択
4. 「投稿設定」で投稿実行
```

**シナリオ 2: YouTube と Niconico を分けて投稿したい**

```
1. 配信元フィルタで「YouTube」を選択
2. YouTube 動画を投稿
3. フィルタをリセット
4. 配信元フィルタで「Niconico」を選択
5. ニコニコ動画を投稿
```

**シナリオ 3: 投稿済み動画を確認したい**

```
1. 投稿状態フィルタで「投稿済み」を選択
2. 投稿済み動画の一覧を表示
3. 投稿日時を確認
```

### 重複投稿防止を有効化して安全に投稿

```
1. settings.env を編集
   PREVENT_DUPLICATE_POSTS=true
2. アプリケーションを再起動
3. 以降、同じ動画の投稿は自動的に検知・防止
```

---

## トラブルシューティング

### Q1: フィルタが反映されない

**A**: 以下を確認してください：

1. **テキスト入力時**
   - テキストボックスにフォーカスがあるか確認
   - 入力後、キーが離される（KeyRelease イベント）まで待つ

2. **ドロップダウン選択時**
   - ドロップダウンで選択を変更したか確認
   - 「全て」で全フィルタをリセット

3. **複合フィルタ**
   - 複数条件を設定した場合、すべてを満たす動画のみ表示
   - 条件を満たす動画がない場合は表示が空

### Q2: フィルタをリセットしたい

**A**: 🔄 **リセット** ボタンをクリックしてください：

```
表示効果:
- タイトル検索欄がクリア
- 投稿状態フィルタが「全て」に
- 配信元フィルタが「全て」に
- すべての動画が表示

ログ出力:
✅ フィルタをリセットしました
```

### Q3: 重複投稿防止が機能しない

**A**: 以下の順序で確認してください：

1. **設定値を確認**
   ```env
   PREVENT_DUPLICATE_POSTS=true
   ```

2. **アプリケーションを再起動**
   - 設定は起動時に読み込まれるため

3. **ログで確認**
   ```
   起動ログ:
   重複投稿防止: 有効
   ```

4. **ドライラン時は実行しない**
   - 本投稿（「投稿設定」ボタン）で確認
   - ドライラン（「投稿テスト」ボタン）では実行しない

### Q4: 重複投稿防止を一時的に無効化したい

**A**: `settings.env` で以下に変更してから再起動：

```env
PREVENT_DUPLICATE_POSTS=false
```

ただし、再投稿時の誤入力にご注意ください。

### Q5: フィルタ適用後、動画が 1 件も表示されない

**A**: 以下の原因が考えられます：

1. **条件が厳しすぎる**
   - フィルタ条件を緩くしてみてください
   - 例: 投稿状態を「全て」に変更

2. **入力値が間違っている**
   - タイトル検索の大文字小文字は区別しません
   - 部分一致なので、完全な単語でなくても OK

3. **DB に該当する動画がない**
   - 「全て」で全動画を表示してから、条件を追加

## 技術詳細

### GUI フィルタ実装

#### ファイル: [gui_v3.py](../../gui_v3.py)

**フィルタパネル UI の追加**:
- 行 75-115: `setup_ui()` メソッド内でフィルタパネルを追加
- フィルタパネル構成：
  - タイトル検索 Entry ウィジェット
  - 投稿状態 Combobox（全て/投稿済み/未投稿）
  - 配信元 Combobox（全て/YouTube/Niconico）
  - リセット Button

**フィルタロジック実装**:
- 行 185-271: `apply_filters()` メソッド
  - タイトルで部分一致フィルタ（大文字小文字区別しない）
  - 投稿状態で完全一致フィルタ
  - 配信元で完全一致フィルタ
  - 3 つの条件を AND で結合
  - フィルタ後の動画を Treeview に表示

**データキャッシュ**:
- 行 130-133: `__init__()` メソッド
  - `self.all_videos`: DB から読み込んだ全動画
  - `self.filtered_videos`: フィルタ適用後の動画

**リセット機能**:
- 行 272-277: `reset_filters()` メソッド
  - すべてのフィルタ UI をクリア
  - `apply_filters()` を呼び出して全動画を再表示

### 重複投稿防止実装

#### ファイル: [config.py](../../config.py)

PREVENT_DUPLICATE_POSTS 設定の読み込み：

```python
# PREVENT_DUPLICATE_POSTS 環境変数を読み込み
prevent_duplicate_posts = os.getenv("PREVENT_DUPLICATE_POSTS", "false").lower() in ["true", "1", "yes", "on"]

# ログ出力
if prevent_duplicate_posts:
    logger.info("🛡️ 重複投稿防止: 有効")
else:
    logger.info("重複投稿防止: 無効")
```

#### ファイル: [database.py](../../database.py)

重複投稿検知メソッド：

```python
def is_duplicate_post(self, video_id: str) -> bool:
    """
    重複投稿かどうかをチェック

    Args:
        video_id: 動画ID

    Returns:
        bool: 重複投稿の場合 True、初回投稿の場合 False
    """
    try:
        conn = self._get_connection()
        cursor = conn.cursor()

        cursor.execute("""
            SELECT COUNT(*) FROM videos
            WHERE video_id = ? AND posted_to_bluesky = 1
        """, (video_id,))

        count = cursor.fetchone()[0]
        conn.close()

        is_duplicate = count > 0
        if is_duplicate:
            logger.warning(f"⚠️ 重複投稿検知: この動画は既に投稿済みです（{video_id}）")

        return is_duplicate

    except Exception as e:
        logger.error(f"重複チェック中にエラーが発生: {e}")
        return False
```

#### ファイル: [gui_v3.py](../../gui_v3.py)

投稿前の重複チェック：

```python
def _execute_post(self, dry_run=False):
    """投稿実行メソッド"""
    # ...
    video = self.get_selected_video()
    if not video:
        return False

    # ★ 重複投稿防止チェック
    if self.config.prevent_duplicate_posts and not dry_run:
        if self.db.is_duplicate_post(video["video_id"]):
            # 警告メッセージボックスを表示
            messagebox.showwarning(
                "⚠️ 重複投稿防止",
                f"この動画は既に投稿済みです。\n\n【 {video['title']} 】\n\n重複投稿を防止するため、投稿を中止しました。"
            )
            return False  # 投稿中止

    # 通常投稿を実行
    if self.plugin_manager:
        success = self.plugin_manager.post_video_with_all_enabled(video, dry_run=dry_run)
    elif self.bluesky_core:
        success = self.bluesky_core.post_video_minimal(video)
    else:
        logger.error("投稿エラー: プラグインが見つかりません")
        return False

    # 投稿成功時に DB を更新
    if success and not dry_run:
        self.db.mark_as_posted(video["video_id"])

    return success
```

---

## 関連ドキュメント

- [モジュール一覧](../References/ModuleList_v3.md) - GUI フィルタと重複防止の実装詳細
- [設定概要](../Guides/SETTINGS_OVERVIEW.md) - PREVENT_DUPLICATE_POSTS の詳細
- [デバッグ・ドライラン](./DEBUG_DRY_RUN_GUIDE.md) - ドライランモードの使い方
- [将来ロードマップ](../References/FUTURE_ROADMAP_v3.md) - v2.3.0 以降の計画

---

**作成日**: 2025-12-19
**最後の修正**: 2025-12-19
**ステータス**: ✅ 完成・本番対応

---

## 関連ドキュメント

- [GUIの使い方](v3/docs/Guides/GUI_USER_MANUAL.md)
- [設定ファイル例](../../settings.env.example)
- [デバッグ・ドライランガイド](DEBUG_DRY_RUN_GUIDE.md)

---

**作成日**: 2025-12-19
**最後の修正**: 2025-12-19
**ステータス**: ✅ 完成・検証済み
